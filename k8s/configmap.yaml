apiVersion: v1
kind: ConfigMap
metadata:
  name: newssummariser-config
  labels:
    app: newssummariser
data:
  # API Configuration
  API_HOST: "0.0.0.0"
  API_PORT: "8000"
  STREAMLIT_PORT: "8501"
  STREAMLIT_HOST: "0.0.0.0"

